import Dashboard from '../Dashboard.jsx';

export default function ProDashboard() {
  // Reuse the unified enhanced Dashboard. It reads the user and adapts to professional mode.
  return <Dashboard />;
}
