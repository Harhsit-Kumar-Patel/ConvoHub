import Dashboard from '../Dashboard.jsx';

export default function EduDashboard() {
  // Reuse the unified enhanced Dashboard. It reads the user and adapts to educational mode.
  return <Dashboard />;
}
